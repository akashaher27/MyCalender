<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Base Theme -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- toolbar theme -->
    <style name="toolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <!--text-->
    <style name="textHeadLine" parent="TextAppearance.AppCompat.Headline"></style>
    <style name="textSubhead" parent="TextAppearance.AppCompat.Subhead"></style>
    <style name="textLarge" parent="TextAppearance.AppCompat.Large"></style>
    <style name="textTitle" parent="TextAppearance.AppCompat.Title"></style>
    <style name="textMedium" parent="TextAppearance.AppCompat.Medium"></style>
    <style name="textSmall" parent="TextAppearance.AppCompat.Small"></style>

</resources>
